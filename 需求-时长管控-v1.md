# 时长管控 · 需求整理 v1（已实现）

> 状态：**已按此实现并打包**。确认日期 2026-09-13，实现日期 2026-09-13。
> 涉及模块：番茄节奏（已有，加了开关）+ 每日额度（新增）
>
> 实现时的两处补充（需求文档里没写、落地时定的）：
> - 每日额度**默认关闭** —— 装上就被限时、孩子以为软件坏了，是最差的第一印象
> - 「练习时长统计」也改成活跃计时（原来墙钟，挂机也算），跟额度口径统一

---

## 0. 一句话结论

你这次提的需求，本质是 **在已有的「番茄节奏」之外，再加一层「每日总量额度」**。
这俩是不同维度的事，得拆开做，不能混成一个参数：

| | 番茄节奏（已有） | 每日额度（**新增**） |
|---|---|---|
| 管什么 | **节奏** —— 学多久、歇多久 | **总量** —— 今天一共只能用多久 |
| 参数 | 工作 20 分 / 休息 10 分 | 每天 2 小时 |
| 计时方式 | **墙钟倒计时**（20 分钟就是 20 分钟） | **活跃计时**（按键驱动，发呆不算） |
| 到点行为 | 锁屏强制休息，倒计时结束自动解锁 | 锁屏，**必须密码**，不会自动恢复 |
| 能绕过吗 | 不能（停番茄要密码） | 不能（**重启程序也算数**） |
| 开关 | 可开 / 可关（要密码） | 可开 / 可关（要密码） |

**为什么要拆开**：如果只有一个"总时长"，孩子打 20 分钟想歇一下就没得歇；
如果只有番茄节奏，孩子可以一直循环打一整天。两个都要，才是完整的。

---

## 1. 功能结构

```
家长面板（进这个页面本身就要密码）
└── 时间管理
    ├── ① 番茄节奏            [ 开 / 关 ]
    │     ├ 工作时长       20 分钟   （1~120 可调）
    │     └ 休息时长       10 分钟   （1~120 可调）
    └── ② 每日时长限制         [ 开 / 关 ]
          ├ 每天上限       2 小时    （可调）
          ├ 今日已用       00:37
          └ [ 重置今日用量 ]  ← 家长手动清零

主界面顶栏：  今日剩余  01:23:00    ← 只在②开启时显示
```

**要点**
- 两个开关**互相独立**（已确认）：可以只要节奏、可以只要额度、也可以都要。
- 两个开关的切换**都要密码**（在家长面板里，等于天然要密码）。
- 主界面**不出现**任何"改设置"入口，孩子看不到参数、也改不了。

---

## 2. 每日额度怎么运转

### 2.1 计时（已确认：只算实际练习时间）

**活跃窗口机制**
- 每次按键 → 刷新一个 **15 秒**的活跃窗口，窗口内计时器持续累计
- 窗口过期（15 秒内没有按键）→ 计时暂停
- 连续打字时无缝累计；发呆、离开、翻看说明页 → 自动不算

**和番茄钟的关系**（重要）
- 额度 = **活跃计时**，按键驱动
- 番茄 = **墙钟倒计时**，不参与额度计算
- 两者独立。副作用：番茄跑了 20 分钟，额度可能只扣了 12 分钟 —— 这是对的，
  发呆不该收钱。

**持久化**
- 累计值实时落盘 `data/progress.json`，中途关程序不丢，重开接着算

### 2.2 提醒节点

| 剩余 | 表现 |
|---|---|
| 10 分钟 | 顶栏剩余时间**变橙** |
| 5 分钟 | 顶部横幅：「快到今天的练习时间了，把这一组打完」 |
| 1 分钟 | 响铃一次 |
| 0 | **锁屏** |

### 2.3 用完 → 锁屏

- 全屏锁死，键盘鼠标失效（和番茄休息锁屏一样的做法）
- 文案：「**今天的练习时间用完啦**」「今天已经练了 2 小时 00 分」，
  不显示倒计时（因为不会自己恢复）
- 底部留一个不显眼的「家长解锁」按钮

### 2.4 密码解锁对话框

```
┌─────────────────────────────────────────┐
│  今天的练习时间用完啦                    │
│  今天已经练了 2 小时 00 分               │
│                                          │
│  家长解锁 —— 今天还能再用：              │
│   ( ) 30 分钟    ( ) 1 小时              │
│   ( ) 2 小时     ( ) 5 小时              │
│   ( ) 今天不限制                         │
│   自定义： [_____] 分钟                  │
│                                          │
│            [取消]      [解锁]            │
└─────────────────────────────────────────┘
```

**语义**：选「1 小时」= **今天还能再用 1 小时**（不是"再加 1 小时到原额度上"）。
程序内部换算成 `今日上限 = 今日已用 + 1 小时`，对孩子和对家长都是这句话最直观。

**「今天不限制」只对今天有效** —— 明天照常恢复 2 小时。

### 2.5 跨天重置（已确认：自然日 0 点）

- 每天 0 点：`used_seconds` 归零、`unlimited` 复位、`grants` 归零
- **程序运行中也会检测**，不靠重启触发
- 已知取舍：理论上熬夜到 0 点后额度会刷新。实际影响很小 —— 因为计时口径是
  「实际练习时间」，必须真的在打字才累计。

### 2.6 锁屏优先级

番茄休息锁屏和额度用完同时成立时：**额度用完 > 番茄休息**。

理由：番茄休息 5~10 分钟就过去了，额度用完是"今天到此为止"，
两者叠在一起时应该显示更严重的那个，免得孩子以为等一会儿就能接着打。

---

## 3. 已确认的决定

| # | 问题 | 结论 |
|---|---|---|
| ① | 「2 小时」算的是什么时间 | **只算实际练习时间**（15 秒活跃窗口，按键驱动） |
| ② | 跨天几点重置 | **自然日 0 点** |
| ③ | 两个开关的关系 | **两个独立开关**（番茄节奏 / 每日额度） |
| ④ | 计时起点 | 第一次按键（由 ① 推导，不必单独设） |

---

## 4. 边界情况（实现时必须覆盖）

- [x] **重启绕过** —— 启动时先查今日已用，超了直接进锁屏，不让孩子靠重启续命
- [x] **关程序** —— 已用时长实时落盘，不丢
- [x] **家长加时** —— 只对当天有效，第二天恢复满额
- [x] **番茄中途到额度** —— 番茄立即中断，转额度锁屏
- [x] **额度用完时正在休息** —— 休息照常结束，但结束后不解锁（额度优先）
- [x] **系统时间被改** —— 不管。7 岁半不会，为此加防作弊逻辑不值当
- [x] **当天多次加时** —— 家长面板显示「今日已用 X 分钟 / 加时 Y 次」，你能看到自己放了多松
- [x] **关了额度开关** —— 今天已记录的时间保留，重新打开接着算
- [x] **0 点跨天时程序正在走** —— 定时器里检测日期变化，当场复位

---

## 5. 落盘字段

`data/progress.json` 新增一段：

```json
"usage": {
  "date": "2026-09-13",
  "used_seconds": 1320,
  "quota_seconds": 7200,
  "unlimited": false,
  "grants": 1
}
```

`data/config.json`：

```json
"pomodoro":    { "work": 20, "break": 10, "enabled": true },
"daily_limit": { "enabled": true, "minutes": 120, "idle_grace_sec": 15 }
```

---

## 6. 改动范围（实现时）

| 文件 | 改什么 |
|---|---|
| `store.py` | 新增 `usage` 落盘 + 跨天检测 + 默认配置 |
| `app.py` | 活跃计时器、顶栏剩余时间、额度锁屏、启动检查 |
| `dialogs.py` | 家长面板「时间管理」页 + 解锁加时对话框 |
| `courses.py` | 说明页补一句「练习时间用完要休息」 |
| `README.md` | 补时长管控说明 |

**不动**：打字引擎、键盘、题库导入、课程内容。
